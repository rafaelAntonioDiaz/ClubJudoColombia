# ===================================================
# 1. CONFIGURACIÓN DE BASE DE DATOS (H2 FILE-BASED)
# ===================================================

# Usamos 'file:./data/judo_db' para que H2 guarde la información en un archivo físico
# dentro de una carpeta llamada 'data'. Esto es VITAL para que los datos no se borren en Railway.
spring.datasource.url=jdbc:h2:file:./data/judo_db;DB_CLOSE_ON_EXIT=FALSE;AUTO_RECONNECT=TRUE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# Habilitar la consola web de H2 (Para ver tus tablas en el navegador)
# Podrás acceder a ella entrando a: http://localhost:8080/h2-console
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# ===================================================
# 2. CONFIGURACIÓN DE JPA / HIBERNATE
# ===================================================

spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ===================================================
# 3. SERVICIO DE CORREO (MAILTRAP - PRUEBAS)
# ===================================================
spring.mail.host=sandbox.smtp.mailtrap.io
spring.mail.port=587
spring.mail.username=96bc855cf7f3e1
spring.mail.password=f53f7ebfb0404a
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.default-encoding=UTF-8
spring.mail.properties.mail.debug=false

# ===================================================
# 4. ALMACENAMIENTO EN LA NUBE (AWS S3 - FREE TIER)
# ===================================================

# Nombre de tu "Caja Fuerte" en AWS (Debe ser un nombre único en todo el mundo)
aws.s3.bucket=club-judo-docs-gratis

# Credenciales de Seguridad de AWS (IAM User)
# IMPORTANTE: En Railway esto se configura como Variables de Entorno, no quemado en el archivo.
aws.accessKeyId=PON_AQUI_TU_ACCESS_KEY
aws.secretAccessKey=PON_AQUI_TU_SECRET_KEY
aws.region=us-east-1